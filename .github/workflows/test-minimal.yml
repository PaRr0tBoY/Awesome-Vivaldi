name: Test Simple Heredoc

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test simple heredoc
        run: |
          cat << 'EOF'
hello world
EOF
          echo "done"
