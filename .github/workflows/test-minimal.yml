name: Test Heredoc

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test heredoc with python
        run: |
          python3 << 'PYEOF'
import sys
print("hello from python heredoc", file=sys.stderr)
PYEOF
          echo "done"
